# rebuildable / generated artifacts
figures/
results/
catalog.sqlite

# per-machine deployment config (copy config.example.json -> config.json)
config.json

# local-only narrative (design docs / verdicts / plans) — kept out of the repo
output/

# per-machine cooldown logbooks + calibration ref — generated on each machine
# (planned: auto-extracted from the PowerPoint decks in config.json ppt_roots)
cooldowns/

# python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/

# os / editor
.DS_Store
.ipynb_checkpoints/
